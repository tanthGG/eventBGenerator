context Refine_Machine_C3
sets
  PKT
  TYPE

constants
  ND
  Dests
  initialSrcAddr
  finalDestAddr
  safetyThreshold
  type
  DATA

axioms
  @ax01 finalDestAddr ∈ ℙ(PKT × ℤ)
  @ax02 ND ⊆ ℕ
  @ax03 finite(ND)
  @ax04 Dests ⊆ ND
  @ax05 finite(PKT)
  @ax06 initialSrcAddr ∈ PKT → ND
  @ax07 safetyThreshold ∈ ℤ
  @ax08 ND ⊆ ℕ ∧ finite(ND)
  @ax09 DATA ∈ TYPE
  @ax10 type ∈ PKT → TYPE

end
